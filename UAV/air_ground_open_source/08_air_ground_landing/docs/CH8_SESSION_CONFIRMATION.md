# CH8 高电平降落请求（当前逻辑）

2026-09-08 恢复高电平语义，取消 CH8 低→高确认。

- RC6 会话有效且本执行器的 GUIDED 跟飞已确认时，新鲜 CH8 高位直接产生降落请求，无需先拨低。
- 提前保持 CH8 高位，在跟飞成立后也会请求降落；实际下降仍受对中、遥测等原有条件限制。
- 低位或中立取消 CH8 请求，缺失/过期/无效时间样本不产生请求。已近地锁存的原生 LAND 仍由原有终端策略处理。
- RC6 的低→高会话授权和人工接管保护保留。人工接管后，仅 CH8 高位不能抢回 GUIDED。
- 原生 LAND 交接可保持已有请求；本次没有增加禁止起飞互锁。

当前文件名为历史沿用，不再代表 CH8 低→高策略。完整 Python 套件 106 项通过，另有 1 项 OpenCV 图像解码测试在不含 OpenCV 的环境中跳过；浏览器方向显示测试另行通过。测试不等价于真实 RC、接管竞态或起降实机验收。
