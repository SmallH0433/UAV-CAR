# Arducam OV9281 on the standard Raspberry Pi 4 CSI connector.
# Explicit overlay is required because camera_auto_detect does not identify
# every third-party OV9281 module. The arducam parameter slows regulator
# startup for modules that need extra power-up settling time.
dtoverlay=ov9281,arducam
